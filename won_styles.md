### hover color themeplate:

```typescript        
        background: isHovered ? colorScheme === 'dark'
                      ? `${theme.colors[theme.primaryColor][8]}40`
                      : theme.colors[theme.primaryColor][0]
                    : 'white', 
 ```                   